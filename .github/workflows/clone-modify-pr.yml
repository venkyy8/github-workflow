name: Main Workflow

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Call reusable workflow
        uses: venkyy8/action-repo@main

